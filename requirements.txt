# BasicModel's requirements (torch, datasets, pytest, executorch, ...) plus the
# WikiOracle-only extras below. Tested interpreter and torch build:
# basicmodel/doc/Installation.md.
-r basicmodel/requirements.txt
requests
fastapi
uvicorn
vector_quantize_pytorch
pyzipper
dropbox
qrcode[pil]
